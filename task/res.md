# Результаты тестирования

| Датасет | Команда | Зависших FSM | Diff с эталоном |
|---------|---------|-------------|----------------|
| ds1 (`test_ds1/`) | `make docker-test1` | 1 | точное совпадение |
| ds2 (`test_ds2/`) | `make docker-test2` | 1 | расхождение в поле duration: эталон `0`, программа `00:00:00.027` |
| ds3 (`datasets/3/`) | `make docker-test3` | 5 | — (эталона нет) |
| ds4 (`datasets/4/`) | `make docker-test4` | 2 | — (эталона нет) |
| ds5 (`datasets/5/`) | `make docker-test5` | 34 | — (эталона нет) |

## Примечание по ds2

`test_ds2/out.csv` содержит `0` в поле duration. Программа выводит `00:00:00.027` —
точное значение по последнему timestamp файла. Расхождение в формате представления,
данные корректны.
